export const API_BASE_URL: string = "http://localhost:3030";

const endpoints = {
  books: "/books",
  authors: "/authors",
  users: "/auth",
  sliders: "/sliders",
  reviews: "/reviews",
  orders: "/orders",
};

export default endpoints;
